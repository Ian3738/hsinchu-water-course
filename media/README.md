# 影片

把兩支影片放進這個資料夾：

- `intro.mp4` — 課前循環播放（固定鏡位，時間流過，15 秒）
- `s1-shangping.mp4` — 第 1 節，水離開的那一段（15 秒）

檔名要跟根目錄 `config.js` 裡的 `media` 對得起來。

mp4 預設不進版控（見 `.gitignore`）。要一起推上 GitHub 的話，
用 Git LFS，或把檔案放到別的空間再把網址填進 `config.js`。
